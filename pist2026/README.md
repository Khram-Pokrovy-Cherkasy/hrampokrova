# 📖 Пам'ятка: Система поминання 2026

Цей проєкт — це високопродуктивний веб-інтерфейс для відображення списків імен (За Здоров'я / За Спокій), інтегрований з Google Таблицями.

## 🚀 Як працює екосистема даних

1.  **Джерело (Google Sheets):** Дані вводяться в таблицю.
2.  **Обробка (Google Apps Script):** Формує JSON-відповідь. Має внутрішній кеш (`CacheService`) на 5 хвилин.
3.  **Шлюз (Cloudflare Worker):** Приховує пряму адресу скрипта, додає CORS-заголовки та кешує дані на рівні Edge-серверів (5 хвилин).
4.  **Frontend (GitHub Pages):** * **Prefetch:** При заході на головну, скрипт фоново завантажує дані.
    * **LocalStorage:** Дані зберігаються в браузері, що дозволяє миттєво відкривати списки.

---

## 🔄 Як примусово оновити дані

Якщо ви внесли зміни в таблицю і хочете побачити їх негайно, пройдіть по черзі через ці рівні:

### 1. Рівень Клієнта (Сайт)
Натисніть на іконку **🔄** поруч із лічильником імен на сторінці списку. Це ігнорує `localStorage` і робить прямий запит до API.

### 2. Рівень Cloudflare (Очищення кешу воркера)
Якщо дані застрягли на рівні Cloudflare, їх можна скинути вручну:
1. Зайдіть у свою панель **Cloudflare**.
2. Перейдіть у розділ **Caching** -> **Configuration**.
3. Натисніть кнопку **Purge Everything** (очистить весь кеш сайту) або **Custom Purge** і введіть URL вашого API (наприклад, `https://api.hrampokrova.pp.ua/?type=health`).
4. Також можна зайти в **Workers & Pages**, обрати свій воркер і в налаштуваннях перевірити активність запитів.

### 3. Рівень Google Scripts
Якщо ви змінили логіку коду в Google:
* Обов'язково зробіть **New Deployment**.
* Старі версії (ID) розгортання в Google кешуються намертво і не оновлюються самі.

---

## 🛠️ Структура та налаштування

### Файлова структура
* `/js/main.js` — Логіка кешування, рендерингу та Prefetch.
* `/components/api-url.js` — Централізоване керування адресою API.
* `/css/style.css` — Система CSS-змінних для кастомізації (теми, шрифти, ширина).

### Збереження налаштувань
Всі зміни користувача (розмір шрифту, обрана тема, ширина контейнера) зберігаються в об'єкті `p2026_settings` у `localStorage`. Вони застосовуються автоматично при кожному завантаженні сторінки через функцію `applySettings()`.

---

## 💡 Поради щодо розробки
* При зміні CSS/JS файлів використовуйте `Ctrl + F5` для скидання кешу браузера.
* Для діагностики використовуйте вкладку **Network** у консолі розробника (F12). Шукайте заголовок `X-Debug-Cache: HIT`, щоб переконатися, що Cloudflare працює коректно.